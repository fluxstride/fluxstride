/** Props for a link that leaves the site. */
export const external = { target: '_blank', rel: 'noopener noreferrer' } as const

/**
 * Sam's buttons are the studio's (button-styles.ts) with 6px corners, as drawn in the
 * personal site's design. Pass alongside `size="inline"` for the 15/600 label.
 */
export const samButton = 'rounded-md'
