import { createCn } from '@fluxstride/design-system/lib/cn'

/** `cn` that also knows this site's type tokens (src/styles/index.css). */
export const cn = createCn([
  'sam-hero',
  'sam-hero-accent',
  'sam-title',
  'sam-title-accent',
  'sam-contact',
  'sam-contact-accent',
  'sam-wordmark',
  'sam-wordmark-accent',
  'sam-quote',
  'sam-index',
  'sam-service',
  'sam-stat',
  'sam-project',
])
